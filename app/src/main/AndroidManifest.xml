<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Shizuku：允许通过 Shizuku 服务执行 shell（可选能力） -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <application
        android:name=".MiniAppApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiniApp"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MiniAppActivity"
            android:exported="false"
            android:launchMode="standard"
            android:documentLaunchMode="intoExisting"
            android:taskAffinity=""
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:label="@string/app_name"
            android:theme="@style/Theme.MiniApp" />

        <activity
            android:name=".ui.PermissionManageActivity"
            android:exported="false"
            android:label="权限管理"
            android:theme="@style/Theme.MiniApp" />


        <activity
            android:name=".ui.DebugActivity"
            android:exported="false"
            android:label="调用日志"
            android:theme="@style/Theme.MiniApp" />

        <activity
            android:name=".ui.WebdavConfigActivity"
            android:exported="false"
            android:label="网络存储"
            android:theme="@style/Theme.MiniApp" />

        <activity
            android:name=".ui.BackupActivity"
            android:exported="false"
            android:label="备份与恢复"
            android:theme="@style/Theme.MiniApp" />

        <activity
            android:name=".ui.AppSettingsActivity"
            android:exported="false"
            android:label="应用设置"
            android:theme="@style/Theme.MiniApp" />

        <provider
            android:name=".provider.MiniAppDocumentsProvider"
            android:authorities="${applicationId}.docs"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <service
            android:name=".dex.DexService"
            android:isolatedProcess="true"
            android:exported="false" />

    </application>

</manifest>
